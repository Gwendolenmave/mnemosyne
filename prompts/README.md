# Public prompt assets

These assets are generic runtime contracts, not a persona corpus. Deployments
may replace their text through the prompt-loading port while preserving the
same output schema, evidence rules, and injection boundary.
